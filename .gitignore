# OS / editor
.DS_Store
.idea/
.vscode/
*.swp

# Never commit real environment files — see backend/.env.example and frontend/.env.example
.env
.env.*
!.env.example

# Dependencies & build output (each app also has its own .gitignore)
node_modules/
vendor/
dist/
